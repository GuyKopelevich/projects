<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- סגנון בסיס לכפתורי מקלדת -->
    <style name="KeyStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">2dp</item>
        <item name="android:background">@drawable/key_background</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">0dp</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:gravity">center</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- סגנון לכפתורים מיוחדים (רווח, אנטר, מחיקה, שפה) -->
    <style name="KeyStyleSpecial" parent="KeyStyle">
        <item name="android:background">@drawable/key_background_special</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>

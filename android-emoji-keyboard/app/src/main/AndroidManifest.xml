<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!--
            שירות IME — הגדרות חובה:
            * android:permission="BIND_INPUT_METHOD" — מאפשר למערכת להתחבר לשירות
            * android:exported="true"               — חובה ב-Android 12+ (API 31+)
            * intent-filter עם android.view.InputMethod — מזהה זאת כמקלדת
            * meta-data עם @xml/method              — מגדיר שפות ו-subtypes
        -->
        <service
            android:name=".EmojiKeyboardService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>

</manifest>
